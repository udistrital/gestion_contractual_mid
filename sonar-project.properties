# === Identificación del proyecto ===
sonar.projectKey=gestion_contractual_mid
sonar.projectName=gestion_contractual_mid
sonar.projectVersion=1.0

# === Directorios del código fuente ===
sonar.sources=src

# === Exclusiones (archivos o carpetas que no se analizan) ===
sonar.exclusions=**/node_modules/**,**/*.spec.ts,**/*.test.ts,**/dist/**,**/coverage/**

# === Configuración de pruebas ===
sonar.tests=src
sonar.test.inclusions=**/*.spec.ts,**/*.test.ts

# === Lenguaje y codificación ===
sonar.sourceEncoding=UTF-8